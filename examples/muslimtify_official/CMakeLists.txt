# Muslimtify official example. Resource paths in main/themes/fonts.h and
# resources/assets/assets.h are relative to the repo root, so run the
# binary from the top-level source dir (see README/CLAUDE.md).

ccompose_add_example(muslimtify_gui main/muslimtify_gui.c)
